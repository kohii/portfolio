---
export type Props = {
	href: string;
};

const { href } = Astro.props;
---

<a
	href={href}
	class="flex items-center p-2 rounded-md text-zinc-300 hover:bg-zinc-800 transition-colors duration-200 gap-3"
	target="_blank"
	rel="noopener noreferrer"
>
	<slot />
</a>
